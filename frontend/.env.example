# TankVitals — configuração do frontend (FE-01)
# Copie para .env. Só variáveis com prefixo VITE_ chegam ao navegador.

# Vazio = usa o proxy de desenvolvimento do Vite (recomendado, evita CORS).
# Preencha apenas se o backend estiver em outro host/porta.
VITE_API_BASE_URL=
